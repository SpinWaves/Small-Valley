// Copyright (C) 2022 SpinWaves (https://github.com/SpinWaves)
// This file is a part of "Small Valley"
// For conditions of distribution and use, see the LICENSE
//
// Author : kbz_8 (https://solo.to/kbz_8)

#ifndef __TEXT__
#define __TEXT__

/*
#include <pch.h>
enum class color_text { white, black };
enum class align { left, center, right };

class Text
{
    public:
        Text(TTF_Font *font, color_text color = color_text::black);
        Text(const char* path, size_t size, color_text color = color_text::black);

        void init(std::string text);
        void render(int x, int y, align alignent = align::left);

        void free_texture();

        ~Text();

    private:
        void add_line(const char* text);

        TTF_Font *_font = nullptr;
        bool _is_local_font = false;

        SDL_Color _color;

        GLuint _vbo = 0;
        GLuint _vao = 0;

        int _vertex_count = 0;
};
*/
#endif // __TEXT__
